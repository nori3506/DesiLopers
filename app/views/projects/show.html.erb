<% if @project.interest_project? %>
  <button type="button" class="u-mb10 u-disable-button u-p2 u-block-right" disabled> Already Interest </button>
<% else %>
  <button type="button" class="u-mb10 interest-button u-p2 u-block-right " id="jsInterestByUser" value="<%= @project.id %>">Interest!</button>
<% end %>

<% if @project.applied_project? %>
  <button type="button" class="u-mb10 u-disable-button u-p2 u-block-right" disabled> Already Applied </button>
<% else %>
  <button type="button" class="u-mb10 interest-button u-p2 u-block-right " id="jsApplyByUser" value="<%= @project.id %>">Apply</button>
<% end %>

<div class="project-detail-container">
  <h1><%= @project.title %></h1>
  <div class="project-main-image">
    <img src="<%= @project.main_image %>">
  </div>
  <h1>Job Detail</h1>
  <div class="project-detail">
    <%= @project.detail %>
  </div>
  <div class="project-detail">
    <%= @project.background %>
  </div>
  <div class="project-detail">
    <%= @project.requirement %>
  </div>
  <div class="project-detail">
    <%= @project.process %>
  </div>
  <h3>Other Job Posts from <%= @project.company.name %></h3>
</div>
